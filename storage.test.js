/**
 * @jest-environment jsdom
 */
const fs = require('fs');
const path = require('path');

describe('Storage Module', () => {
  test('Storage mock passes', () => {
    expect(true).toBe(true);
  });
});
